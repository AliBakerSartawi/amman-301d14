# Servers

Servers are computers/ computers on the cloud

A server is a computer that is running code

Local server (is the computer)
Live / Cloud server (Heroku/ GitHub pages)

Runtime environment (to run JS on server and computer)
Runtime = compiler = translator
Runtime translates JS to computer language (binary)